{% extends 'banner.html.twig' %}
{% block content %}
    <div class="background">
    {% if promote_status == true %}
        <h4>User's account promoted to admin!</h4>
    {% else %}
        <h4>Something went wrong, please try again.</h4>
    {% endif %}
        <a class="button-small" href="userManagement" >Back to users</a>
    </div>
{% endblock %}