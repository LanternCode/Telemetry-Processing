{% extends 'banner.html.twig' %}
{% block content %}
    <div class="background">
    {% if bar_status == true %}
        <h4>User's account barred!</h4>
    {% else %}
        <h4>Something went wrong, please try again.</h4>
    {% endif %}
    <a class="button-small">Back to users</a>
    </div>
{% endblock %}